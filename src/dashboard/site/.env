# Can have an other file called `.env.local` to overwrite

# Exposed to node and site
NEXT_PUBLIC_NODE_ENV = "production"
NEXT_PUBLIC_PORT = 5000

# Just site
MONGO_URI_DEV = ""
MONGO_URI_PRODUCTION = ""